@{
    ViewData["Title"] = "Accès Refusé";
    Layout = "_DashboardLayout"; // Utilise le même beau layout que le dashboard
}

<div class="container-fluid mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg border-0 rounded-lg">
                <div class="card-header bg-danger text-white text-center py-5">
                    <i class="fas fa-ban fa-5x mb-3"></i>
                    <h2 class="mb-0">Accès Refusé</h2>
                </div>
                <div class="card-body text-center p-5">
                    <h4 class="text-muted">
                        Vous n'avez pas les autorisations nécessaires pour accéder à cette page.
                    </h4>
                    <p class="mt-4">
                        Cette section est réservée aux administrateurs et aux médecins.
                    </p>
                    <div class="mt-5">
                        <a href="/Home/Dashboard" class="btn btn-primary btn-lg px-5">
                            <i class="fas fa-home me-2"></i> Retour au Tableau de Bord
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>